/**
 * Deepest category level shown in the storefront nav mega-menu.
 * Depth 1 = root (sidebar), 2 = child, 3 = grandchild.
 *
 * Deeper levels remain in the catalog model (backend allows more) and are
 * reachable via PLP, breadcrumbs, and `/categories`.
 */
export const STOREFRONT_NAV_CATEGORY_MAX_DEPTH = 3;
