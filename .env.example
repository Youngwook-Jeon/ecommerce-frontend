# Stripe.js publishable key (Dashboard → Developers → API keys, Test mode)
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_xxx

# Secret key belongs on payment-service only (ecommerce-msa/.env):
# PAYMENT_PROVIDER=stripe
# STRIPE_API_KEY=sk_test_xxx
# STRIPE_WEBHOOK_SECRET is auto-injected by make apps / make up (stripe listen --print-secret)
